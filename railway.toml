[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["openjdk21", "maven"]

[phases.build]
cmds = ["mvn -DskipTests clean package -Pproduction"]

[start]
cmd = "java -Dserver.port=$PORT -jar target/*jar"
